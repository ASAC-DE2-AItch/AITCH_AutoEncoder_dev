# ============================================================
#  AITCH · AE 트랙 — Python 의존성  (근거: AE_개발계획_v1.md §4 venv)
#  대상 Python: >= 3.10   (계획서 §4)
#  작성: 세그 / 2026-07-14
# ------------------------------------------------------------
#  [재현성 2단계 — 이 파일은 "설치 명세(호환범위)"다]
#    1) pip install -r requirements.txt
#    2) pip freeze > requirements.lock.txt     # ← 실제 버전 완전 고정(재현성 잠금)
#  노트북 P0-1이 찍는 런타임 스냅샷을 REPORT에 함께 기록하면 감사 추적 완결.
#  ('==' 강제 pin을 여기 두지 않은 이유: 네 로컬 venv 실측 버전을 세그가 볼 수
#   없어 임의 pin은 오히려 설치 충돌을 부른다 — 정확한 pin은 위 2)의 lock에서.)
# ------------------------------------------------------------
#  [설치 범위]
#    · ae_eda_v1(P1 EDA)이 실제로 import 하는 필수 = numpy · pandas · pyarrow
#    · 나머지는 계획서 §4 표준 venv — P4(ae_v1) 모델링부터 사용
# ============================================================

# ── EDA 필수 (ae_eda_v1가 실제 import) ──────────────────────
numpy>=1.26,<3
pandas>=2.1,<4
pyarrow>=14.0          # parquet 캐시 엔진 (P0-3) — 없으면 로딩 셀에서 실패

# ── 모델링 표준 (P4 ae_v1~ 부터 · 계획서 §4) ────────────────
scikit-learn>=1.3      # PCA·Mahalanobis 참조 베이스라인 (§8-1)
matplotlib>=3.8        # 학습곡선·score 플롯 (P4~)
optuna>=3.5            # HPO — 목적함수는 VAL loss만 (§7-3 오염방지)
pyyaml>=6.0            # config/ae_params_v1.yaml (§11-3)
mlflow>=2.11           # 실험 관리 (계획서 §4 '권장' — 선택. 빼려면 이 줄 주석처리)

# ── torch: CUDA 때문에 requirements에서 제외 (의도적) ───────
#   pip install -r 이 CPU 빌드로 GPU torch를 덮어쓰지 않게 여기서 주석 처리.
#   이미 GPU venv에 torch가 있으면 그대로 두고, 새로 깔 때만 아래로:
#     CUDA(권장): pip install "torch>=2.2" --index-url https://download.pytorch.org/whl/cu121
#     CPU 폴백  : pip install "torch>=2.2"
#   (cu121 등 CUDA 태그는 로컬 드라이버에 맞춰 조정 — 계획서 §4 GPU 규칙)
# torch>=2.2
